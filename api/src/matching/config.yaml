algorithm:
  min_time_overlap_minutes: 10
  min_overlap_distance_meters: 250.0
  overlap_tolerance_meters: 120.0
  overlap_weight: 0.7
  interest_weight: 0.3
  shared_meters_per_minute: 80.0

service:
  pass_cooldown_days: 0 # Normally 7 days, 0 for demo
  queue_assignment_days_ahead: 1

